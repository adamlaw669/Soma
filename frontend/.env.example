# Backend API URL (FastAPI server)
NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com

# Doctor portal API key (must match DOCTOR_API_KEY on the backend)
# This is a SERVER-SIDE only variable — do NOT prefix with NEXT_PUBLIC_
DOCTOR_API_KEY=change-me-to-match-backend-doctor-api-key
